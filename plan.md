# Current Plan

1. Keep Bun as the single package manager.
2. Keep German routes canonical and redirect old English paths.
3. Preserve only archive content with clear provenance.
4. Run `bun run lint`, `bun run test`, and `bun run build:static` before release.
5. Add new historical material with source notes in `docs/CONTENT.md`.
